{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "node --import tsx ./src/hello.ts",
    "watchdog": "node --import tsx ./src/watchdog.ts",
    "perf": "node --import tsx ./src/perf.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "playwright-core": "^1.59.1",
    "tsx": "catalog:"
  }
}
